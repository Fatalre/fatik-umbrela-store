services:
  app_proxy:
    environment:
      APP_HOST: server
      APP_PORT: 3000

  server:
    image: fatalre/fatik-video-library:0.1.0
    restart: unless-stopped
    environment:
      PORT: 3000
      NODE_ENV: production
    volumes:
      - ${APP_DATA_DIR}/data:/data